# Optimizer backlog

One item per day, in order. The optimizer robot takes the first unchecked item,
ships it, and marks it `[x] YYYY-MM-DD` in the same commit as the change.
`[!] blocked: <reason>` means the build failed twice and the item was skipped.

- [x] 2026-09-08 Featured creators, part 1: `src/data/creators.ts` (slug, name, type, location, website, oneLiner, bio, tags, workLinks[], contests[], featuredSince) seeded with Ruminate X. Build `/creators` (grid of cards) and `/creators/[slug]` (profile with a dofollow link to the website, a "Featured creator" pill, work links, and a "Get featured" mailto CTA). Add /creators to the homepage nav/footer and sitemap.ts. Person/Organization JSON-LD on the profile.
- [x] 2026-09-09 Featured creators, part 2: the backlink loop. Badge at `/badge/featured-creator.svg` (dark, indigo accent, "Featured on AI Film Contests") and a copy-paste embed snippet on every profile. Add a "Why be featured" section on /creators.
- [x] 2026-09-11 Featured creators, recurring: added Ariel Kotzer (arielko) from `reports/creator-candidates.md` to creators.ts. At most twice a week; otherwise continue down this list. Never invent a creator or a fact about them.
- [x] 2026-09-10 `/contests/closing-soon`: open contests with a deadline within 14 days, sorted by deadline, with the compact subscribe form. Link from homepage nav/footer and add to sitemap.ts.
- [x] 2026-09-12 `/contests/free`: open contests whose entry fee is free (Free, None, $0, No fee). Same layout.
- [x] 2026-09-13 `/contests/cash-prizes`: open contests whose prize contains a currency amount. Same layout.
- [x] 2026-09-14 Contest page: an inline "Get a reminder 3 days before this deadline" subscribe block next to the deadline.
- [x] 2026-09-15 Point guides at the kept contest rows so the weekly merge can delete the duplicates. In `src/app/guide/[slug]/page.tsx` swapped `astana-ai-film-festival-2026` → `astana-aiff-2026` (2 spots), dropped the duplicate `call-for-films-ai-2026` and `baiff-2026` entries since `we-are-human-festival-call-for-films-2026` and `burano-baiff-italy-2026` were already in the same lists.
- [x] 2026-09-16 `scripts/find-duplicate-contests.mjs`: reads contests from Supabase and prints `DUPLICATE: keep <most complete id> — extras <ids>` for likely-duplicate groups. Three corroborating signals, each gated to avoid false positives (different-year editions, different-city editions, and generic hub-page/host reuse all tested clean): (1) normalized name match within a 60-day deadline window, (2) same non-aggregator domain + exact deadline with real name-token overlap, (3) same normalized URL with either a close deadline (≤14 days) or name overlap. FilmFreeway/Festhome excluded from domain-only matching since 50+ unrelated festivals share those hosts. Read-only, no writes. Ran against live data: found 6 likely duplicate groups (see cron-logs/optimizer-2026-09-16.log for the full list) for the weekly review to act on.
- [x] 2026-09-17 Trust signal: added `updatedAt` (from Supabase `updated_at`) to the `Contest` type and `rowToContest` mapping, and show "Verified <date>" on ContestCard (under the deadline, right column) and on the contest page (in the spec rail, right under the deadline date).
- [x] 2026-09-18 `/contests/new`: open contests added in the last 14 days (`created_at`), newest first, with the compact subscribe form. Added `createdAt` to the `Contest` type and `rowToContest` mapping (from Supabase `created_at`) to support it. Added to sitemap.ts.
- [x] 2026-09-19 Every /topics, /guide, /tools, /vs, /prize, /location, /categories page ends with the compact subscribe form if it does not already. /topics already had it; added `<EmailSubscribe compact />` (same "Deadline alerts" section as topics) to /guide, /tools, /vs, /prize, /location and /categories, just above the "more like this" links.
- [x] 2026-09-20 Contest page: "Related contests" block with 3 open contests sharing a tool or category. The page already had a category-only block; it now ranks open contests by shared categories (weight 2) plus shared named AI tools (Runway, Sora, Kling, Veo, Midjourney, Pika, Luma, ElevenLabs, Higgsfield, Suno, Leonardo; generic "Any AI tools" never counts), then closes-soonest. Heading is now "More open contests like this". Live data check: 82 of 85 open contests get a full three.
- [x] 2026-09-21 IndexNow key file and an IndexNow ping for new pages (then note it for the seo robot in robots/RESEARCH_QUERIES.md). Moved to the front on 2026-09-20: of the eight pages shipped this week, only two (Sep 14 and Sep 16) got an indexing request. GSC was signed out on the Sep 13, 15, 18 and 19 runs and returned "Quota Exceeded" on Sep 17 and Sep 20, so the only signal Google gets is the sitemap. IndexNow does not need a browser session. Shipped: key file `public/2e186ba6faa46d9d63c17d2357c5ee3b.txt` (served at the site root) and `scripts/indexnow-ping.mjs` (`node scripts/indexnow-ping.mjs <url> ...` or `--all` for the whole sitemap, `--dry-run` to preview). Bing, Yandex, Seznam and Naver read IndexNow; Google does not.
- [x] 2026-09-22 Astana pages: rewrite for the click, not the rank. Rewrote `<title>` and meta description on `/topics/astana-ai-film-festival-2026` and `/guide/how-to-submit-to-astana-ai-film-festival-2026` to lead with the answer (submissions closed Sept 7, festival Oct 1–3, $1,000,000 prize) instead of restating the festival name first. No facts changed — both pages already had the numbers verified in their bodies; only title/description reworded.
- [x] 2026-09-23 Repoint the last pages that cite duplicate contest rows. Checked every `featuredContestIds` array (the only place a page actually cites a contest by id — sitemap.ts and the topic/guide entries for astana and we-are-human only matched on their own page-slug strings, not a contest citation, so those stay put; renaming them would break live URLs). Fixed real citations: `runway-ai-film-festival-2026` -> `runway-aif-2026` in 8 `featuredContestIds` arrays across guide/[slug]/page.tsx (one array already had `runway-aif-2026`, so removed the duplicate entry instead of double-listing it) and 1 in topics/[slug]/page.tsx; `turkiye-intl-ai-film-festival-2026` -> `taiff-turkiye-international-ai-film-festival-2026` in 1 topics array; `we-are-human-festival-2026` -> `we-are-human-festival-call-for-films-2026` in 1 topics array. `aif-2026` and any stale id in data/contests.ts were not found — already clean. No direct `/contests/<id>` links existed anywhere. Astana has no dangling contest-id citation left to repoint; its topic/guide pages just happen to share the duplicate's slug text and are legitimate content pages that keep their URLs.
- [x] 2026-09-24 Default Open Graph image (`app/opengraph-image.tsx`) so shared links show a card. Site had no `openGraph.images`/`twitter.images` at all, so every shared link (Slack, Twitter/X, iMessage, Discord) rendered with no preview card. Added a static 1200×630 image via Next's `ImageResponse` (the file-convention route, auto-wired into `layout.tsx`'s metadata — no metadata edit needed): paper background, the same indigo-gradient play-triangle mark as the favicon, and the homepage's own headline/tagline pairing. No live data baked in, so it never goes stale.
- [x] 2026-09-25 `/feature` page: a short "How it works" (pick a contest and pay, we pin it — automatically, usually within minutes, allow up to a day — then it runs 30 days) and a real screenshot of the live homepage spotlight (`public/feature-spotlight-example.png`, captured from the site itself, not a mockup).
- [x] 2026-09-26 Paid featured listings: added an indigo "Featured" pill (next to the existing Free-entry pill) on ContestCard and on the contest page's header band, shown when `contest.featuredUntil` is a future date. `featuredUntil`/`featuredPaidAt` were already on the `Contest` type and Supabase mapping (used by the homepage spotlight); this just surfaces the same flag visually where shoppers browse and land.
- [ ] FAQPage JSON-LD on the homepage: what counts as an AI film, are there free contests, how alerts work.
- [ ] Mobile pass at 375px: homepage hero, ticker and subscribe form must not overflow; tap targets at least 40px.
- [ ] Performance: lazy-load or disable MouseOrbs and BackgroundFX on mobile; make sure no image is unoptimized.
- [ ] `/contests/student`: open contests whose eligibility mentions students. Same layout.
- [ ] Give the deep-page subscribe form a reason to exist. The Sep 19 run put the compact form on every guide, tool, comparison, prize, location and category page, but of this week's 32 signups 28 still came from the homepage and only 4 from a deeper page (/contests/cash-prizes, /contests/tyrannus-angel-ai-film-awards-2026, /contests/african-ai-film-festival-2026, /contests/all-is-prompt-ai-film-festival-2026). Give the compact form a one-line promise and the live open-contest count, the way the homepage form does, and check next week whether the split moves.

When the backlog is empty: check Vercel Analytics for the aifilmcontests project (top
pages, where signups come from) and add three evidence-based items instead of guessing.
- [x] 2026-09-08 Apply the contest-page pattern to /guide/[slug] and /topics/[slug]: tinted header band, prose at a real measure, a sticky rail holding the contests the article cites, and the subscribe block. See DESIGN_LOG.md 2026-09-08 (2).
- [x] 2026-09-08 Same for /tools/[slug], /vs/[slug], /prize/[slug], /location/[slug] and /categories/[slug] — these share InnerLayout, so most of the work is one component.
- [x] 2026-09-08 /creators/[slug]: give creator profiles the same header band treatment, tinted by their work rather than a contest category.
